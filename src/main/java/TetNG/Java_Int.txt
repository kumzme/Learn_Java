Singleton class :: is a class whose only one instance can be created at any given time, in one JVM. A class can be made singleton by making its constructor private.

Equals() method : is defined in Object class in Java and
                   used for checking equality of two objects defined by business logic.
== :or equality operator : in Java is a binary operator, used to compare primitives and objects.
